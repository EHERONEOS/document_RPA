# Wise RPA 当前项目结构说明

> 文档基于当前代码树整理，最后核对日期：2026-09-09。
> 本文描述现行实现，不以历史设计文档或旧版 README 中的目录为准。

## 1. 项目定位

Wise RPA 是一个面向船司单证业务的 Python 浏览器自动化系统。项目当前由三个运行部分组成：

1. **业务 RPA 应用**：从 RabbitMQ 接收任务，解析消息并按队列路由到对应船司业务。
2. **设备侧队列控制 Agent**：在实际执行任务的机器上运行，负责接收中心命令，并为每个已分配队列管理独立 Worker 进程。
3. **中心队列控制平台**：提供维护页面、设备注册、队列分配、暂停/恢复/重启 API，并将设备状态写入 MySQL。

业务执行依赖 RabbitMQ、Redis、Nacos、浏览器和可选的 OSS/验证码/通知服务；中心控制平台额外依赖 MySQL 和 Redis。

## 2. 顶层目录

```text
document_RPA/
├── app/                         # 业务应用和设备侧 Agent
├── capturesdk/                  # Windows 浏览器录屏 SDK 封装及示例
├── queue_control_platform/      # 独立队列控制中心
├── message_list/                # 本地调试和发送用的任务消息样例
├── runtime/                     # 运行时生成目录，不属于源码
├── doc/                         # 项目说明、架构和调试文档
├── docs/superpowers/            # 历史设计稿和实施计划
├── queue_sender.py              # 手工向 RabbitMQ 投递任务的工具
├── funboost_config.py           # Funboost 全局连接配置
├── nb_log_config.py             # 日志相关配置入口
├── pyproject.toml               # Python 依赖和 uv 项目配置
├── .env.example                 # 环境变量模板
└── README.md                    # 安装与启动说明
```

以下目录或文件通常由本地运行产生，不应作为业务源码阅读入口：`.venv/`、`.uv_cache/`、`__pycache__/`、`.pytest_cache/`、`runtime/`、`.idea/`、`.vscode/`、`.trae/` 和 `app/nacos-data/`。

## 3. `app` 业务应用

### 3.1 应用入口

```text
app/
├── main.py                     # 设备侧 Queue Agent 的入口
├── config/                     # 环境变量、Nacos 和 RabbitMQ 配置
├── control/                    # Agent、Supervisor 和 Worker 生命周期
├── queue/                      # RabbitMQ/Funboost 消费和结果消息
├── core/                       # 与船司无关的任务、浏览器、页面和集成能力
├── spider/                     # 按船司组织的登录和业务任务
├── dev/                        # 本地消息调试入口
└── utils/                      # 小型通用工具
```

`app.main` 当前并不直接从环境变量启动固定队列集合，而是转到 `app.control.queue_client.main` 启动设备侧控制客户端。客户端上线后，中心平台通过 Redis Streams 下发队列分配，Agent 再创建对应的 Worker。

### 3.2 配置层：`app/config`

| 文件 | 职责 |
| --- | --- |
| `__init__.py` | 加载项目 `.env`；默认按 `NACOS_ENABLED` 决定是否加载 Nacos 环境配置。 |
| `nacos_config.py` | 从 Nacos 读取共享数据源、RabbitMQ 和爬虫相关配置，并写入环境变量。 |
| `rabbitmq.py` | 定义 `RabbitmqSettings`，统一生成 RabbitMQ 连接参数和 URL。 |
| `settings.py` | 读取浏览器端口、下载目录、队列并发、QPS、账号并发等本地运行参数。 |
| `queue_config.py` | 保留队列名称列表解析逻辑，兼容旧的固定队列启动方式。 |
| `types.py` | 环境变量布尔值转换。 |

配置加载的大致顺序是：加载 `.env` -> 按开关加载 Nacos -> 各运行组件读取自身配置。RabbitMQ 的默认来源是 Nacos；中心平台的 MySQL、Redis、监听地址主要从 `.env` 读取。

### 3.3 设备控制层：`app/control`

```text
app/control/
├── queue_client/
│   ├── main.py                  # Agent 命令行入口
│   ├── client.py                # 注册、心跳、命令消费和事件上报
│   ├── config.py                # 设备 ID、注册令牌、Redis 和项目路径
│   └── redis_client.py          # Redis Streams 命令/事件读写
├── supervisor.py                # 一个队列一个 Worker 进程的监管器
└── worker.py                    # 子进程入口，启动单队列消费者并处理排空
```

核心职责如下：

- `QueueControlClient` 向中心发送心跳，接收 `sync`、`start`、`pause`、`resume`、`restart` 等命令。
- `QueueSupervisor` 为每个队列维护独立的操作系统进程、运行状态、命令队列和事件队列。
- `worker.py` 在子进程中创建 RabbitMQ 消费者；暂停或重启时先停止拉取新消息，再等待已接收任务排空。
- 所有队列 Worker 共享设备级的账号会话协调器，用于限制同一 `websiteInfo.id + 船司` 的浏览器并发和凭据登录竞争。

### 3.4 队列层：`app/queue`

| 文件 | 职责 |
| --- | --- |
| `consumer.py` | 构建单队列消费者，解析任务消息，获取账号浏览器槽位并调用统一分发器。 |
| `booster.py` | 定义项目使用的 Funboost 参数，包括 RabbitMQ、并发数、QPS 和死信参数。 |
| `pausable_rabbitmq.py` | 对 Funboost RabbitMQ 消费循环增加可协作排空能力。 |
| `rabbitmq.py` | 注册携带死信交换机参数的自定义 RabbitMQ Publisher。 |
| `publisher.py` | 对外暴露任务结果发布器。 |
| `message.py` | 校验任务消息并构建 `TaskContext`。 |

单条消息的主要路径是：`consumer.handle_message` -> `build_task_context` -> 获取账号会话槽位 -> `dispatch_context` -> 船司路由 -> 任务类 `run()`。

### 3.5 核心任务层：`app/core`

```text
app/core/
├── task/
│   ├── context.py                # TaskContext、队列名解析和消息内容副本
│   ├── router.py                 # 自动发现 app.spider 下的船司路由
│   ├── dispatcher.py             # 根据 QueueRoute 调用船司 dispatch
│   ├── base_task.py              # 浏览器任务生命周期模板
│   ├── result.py                 # 任务结果数据结构和回传协议
│   └── errors.py                 # 消息、登录、页面和结果回传异常
├── browser/
│   ├── manager.py                # 浏览器启动、复用和关闭管理
│   ├── launcher.py               # 跨平台 Chromium 启动辅助
│   ├── options.py                # 浏览器启动选项
│   ├── port.py                   # 调试端口注册和分配
│   ├── session_lock.py           # profile/账号会话键生成及兼容锁
│   └── window.py                 # 浏览器窗口查找和恢复
├── scheduler/
│   └── account_session.py        # 设备内账号槽位、登录领导者和临时 profile 管理
├── page/
│   ├── dom.py                    # DOM 定位、点击、填写和选择
│   ├── http.py                   # 页面网络请求/响应辅助
│   ├── wait.py                   # 等待和休眠辅助
│   ├── screenshot.py             # 页面截图
│   └── recorder.py               # 浏览器录屏生命周期
├── integrations/
│   ├── notifier.py               # 处理中通知
│   ├── publisher.py              # 任务结果回传
│   ├── oss.py                    # 文件上传
│   ├── captcha.py                # 验证码服务
│   ├── email_otp.py              # 邮箱 OTP 读取
│   └── redis_client.py           # Redis 数据库客户端
└── logging/logger.py             # 项目日志封装
```

`BaseRpaTask.run()` 是业务任务的模板方法，负责初始化浏览器和页面辅助器、登录、录屏、执行业务、失败截图、文件上传以及最终结果发布。船司任务主要实现 `login()` 和 `execute_business()`，并通过选择器和字段校验辅助完成页面操作。

### 3.6 船司业务层：`app/spider`

```text
app/spider/
├── common/
│   └── field_verify.py            # 跨船司字段路径读取、页面值校验和未填字段检查
├── ZIM/
│   ├── base.py                    # ZIM 通用任务基类
│   ├── router.py                  # ZIM 队列路由表和 dispatch
│   ├── selectors.py               # ZIM 页面选择器和字段映射
│   ├── common/login.py            # ZIM 登录
│   ├── common/booking.py          # ZIM 订舱查询
│   └── tasks/
│       ├── qtct_zim_si.py         # QTCT_ZIM_SI
│       └── qtct_zim_vgm.py        # QTCT_ZIM_VGM
└── MSCGW/
    ├── base.py                    # MSCGW 通用任务基类
    ├── router.py                  # MSCGW 队列路由表和 dispatch
    ├── selectors.py               # MSCGW 页面选择器
    ├── common/login.py            # MSCGW 登录
    ├── common/si_field_verify.py  # MSCGW SI 专用字段校验
    └── tasks/fht_mscgw_si.py      # FHT_MSCGW_SI
```

当前统一路由器会扫描 `app.spider` 下的包并加载其 `router.py`。现有路由至少包括：

| 船司 | 队列 | 任务入口 |
| --- | --- | --- |
| ZIM | `QTCT_ZIM_SI` | `qtct_zim_si` |
| ZIM | `QTCT_ZIM_VGM` | `qtct_zim_vgm` |
| MSCGW | `FHT_MSCGW_SI` | `fht_mscgw_si` |

新增船司通常需要新增一个小写目录、`base.py`、`router.py`、`selectors.py`、`common/` 和 `tasks/`，然后在路由表中登记完整队列名。

### 3.7 开发与兼容入口

| 文件 | 用途 |
| --- | --- |
| `app/dev/local_runner.py` | 从本地 JSON 消息构建上下文并执行路由，适合不连接 RabbitMQ 的流程调试。 |
| `app/dev/template_loader.py` | 读取本地消息模板。 |
| `app/control/worker.py` | 被 Supervisor 调起的单队列子进程入口。 |
| `app/queue/consumer.start_consumers` | 旧的同进程多消费者兼容入口，当前正式 Agent 路径不使用。 |

## 4. 中心控制平台：`queue_control_platform`

```text
queue_control_platform/
├── server/
│   ├── main.py                  # Uvicorn/FastAPI 启动入口
│   ├── web.py                   # API、维护页面和生命周期管理
│   ├── config.py                # MySQL、Redis、Host、Port 配置
│   ├── repository.py             # MySQL 表结构和设备/队列/命令/状态持久化
│   ├── redis_bus.py              # Redis Streams 命令和事件总线
│   └── event_processor.py        # 消费 Agent 事件并落库，心跳后下发分配快照
├── frontend/
│   ├── templates/index.html      # 维护页面 HTML
│   └── static/                   # 页面 JavaScript 和 CSS
└── docker/compose.yml            # 本地 MySQL 8.4、Redis 7.4
```

中心平台的数据和通信边界：

- MySQL 保存设备、队列绑定、期望状态、控制命令和最新状态快照。
- Redis Stream `queue-control:commands:<DEVICE_ID>` 保存发往指定设备的命令。
- Redis Stream `queue-control:events` 保存 Agent 的心跳、队列状态和命令结果。
- `EventProcessor` 只有在事件处理完成后才确认 Redis 消息；设备重新心跳后，中心发送完整分配快照帮助 Agent 恢复。

## 5. 录屏 SDK：`capturesdk`

```text
capturesdk/
├── client.py                    # Capture SDK Python 客户端和会话对象
├── framework_helpers.py         # 根据浏览器 PID/窗口句柄选择录制目标
├── third_party/ffmpeg/          # Windows FFmpeg 可执行文件和 DLL
├── drissionpage_demo.py         # DrissionPage 录屏示例
├── playwright_demo.py           # Playwright 录屏示例
└── selenium_record.py           # Selenium/Chrome 录屏示例
```

正式任务生命周期主要通过 `app/core/page/recorder.py` 接入；`capturesdk` 中的 demo 更适合验证 Windows 浏览器窗口和录屏 SDK 能力，不是业务路由入口。

## 6. 根目录辅助文件

| 文件/目录 | 用途 |
| --- | --- |
| `message_list/` | 本地任务 JSON，例如 `QTCT_ZIM_SI`、`FHT_MSCGW_SI` 和 VGM 样例。 |
| `queue_sender.py` | 读取 JSON、规范化队列名并向 RabbitMQ 投递单条或批量任务。 |
| `funboost_config.py` | 将 `RabbitmqSettings` 暴露给 Funboost。导入时会触发 RabbitMQ 配置加载。 |
| `pyproject.toml` | Python 3.12.12、DrissionPage、Funboost、FastAPI、Redis、MySQL 等依赖。 |
| `.env.example` | 本地配置模板，包括控制平台、浏览器、并发和 Nacos 开关。 |
| `runtime/` | 下载文件、浏览器 profile、截图、日志和已保存任务消息。 |
| `doc/` | 当前架构、并发、状态维护、设计需求和变更记录。 |

## 7. 运行关系

### 7.1 生产/队列模式

```text
中心维护页面
    │ FastAPI + MySQL
    │ Redis Streams
    ▼
设备 Queue Agent（app.main）
    │ QueueSupervisor：一个队列一个进程
    ▼
单队列 Worker（app.control.worker）
    │ PausableRabbitmqConsumer + Funboost
    ▼
RabbitMQ
    │ task JSON
    ▼
TaskContext → 动态船司路由 → BaseRpaTask → 浏览器/页面操作
    │
    ├── 处理中通知
    ├── 截图、录屏和 OSS 文件
    └── 任务结果回传
```

### 7.2 单条任务生命周期

1. Worker 从 RabbitMQ 接收 JSON，并由 `build_task_context()` 校验队列名、消息 ID、站点信息和业务内容。
2. `AccountSessionCoordinator` 为 `websiteInfo.id + carrier` 分配浏览器槽位和 profile。
3. `resolve_queue_route()` 扫描船司路由表，确定客户、船司、业务和具体 handler。
4. `BaseRpaTask.run()` 启动/接管浏览器，执行登录和业务操作。
5. 失败时尽力截图；任务结束时停止录屏、上传过程文件，并由 `ResultPublisher` 发布结果。
6. Consumer 释放账号会话租约，RabbitMQ 消息进入正常 ACK/NACK 生命周期。

## 8. 关键配置和启动入口

### 8.1 启动入口

```bash
# 本地业务消息调试
uv run python -m app.dev.local_runner --message ./message_list/msg_demo.json

# 启动设备侧 Queue Agent
uv run python -m app.main

# 启动中心控制平台
uv run python -m queue_control_platform.server.main

# 启动本地 MySQL 和 Redis
docker compose -f queue_control_platform/docker/compose.yml up -d
```

### 8.2 重要环境变量

| 配置 | 作用 |
| --- | --- |
| `NACOS_ENABLED` | 是否加载 Nacos 环境配置。 |
| `RABBITMQ_CONFIG_SOURCE` | RabbitMQ 配置来源标识；当前默认流程仍由 Nacos 提供连接信息。 |
| `QUEUE_CONTROL_DEVICE_ID` / `QUEUE_CONTROL_DEVICE_TOKEN` | 设备 Agent 注册身份。 |
| `QUEUE_CONTROL_REDIS_URL` | Agent 与中心平台共用的 Redis 地址。 |
| `QUEUE_CONTROL_MYSQL_URL` | 中心平台 MySQL 连接地址。 |
| `QUEUE_CONCURRENT_NUM` / `QUEUE_QPS` | 单队列 Funboost 并发数和启动速率。 |
| `ACCOUNT_MAX_CONCURRENT` | 同一设备内账号会话的最大浏览器槽位数。 |
| `BROWSER_PORT_START` / `BROWSER_PORT_END` | 浏览器调试端口池。 |
| `ENABLE_BROWSER` | 本地配置项；本地骨架调试时通常关闭真实浏览器。 |

## 9. 当前维护注意事项

1. 当前正式队列发现方式是“中心平台分配给设备”，不能把 `RPA_QUEUES` 当作 Agent 的唯一配置来源；旧的固定队列入口仍保留用于兼容。
2. 当前真实船司目录是 `app/spider/ZIM` 和 `app/spider/MSCGW`。历史文档中出现的 `app/Spider/WHL`、`FL_WHL` 等路径属于旧架构或历史设计，不能作为当前代码入口。
3. 根目录未发现 `tests/` 或 `test_*.py` 测试文件；修改队列控制、账号会话或任务生命周期时，应补充自动化测试，并至少通过本地消息调试验证路由。
4. `runtime/` 下可能含账号 profile、任务消息、截图和日志，提交代码或分享日志前应检查敏感信息。
5. RabbitMQ 队列声明包含死信交换机参数，修改 `app/queue/booster.py` 或 `app/queue/rabbitmq.py` 前需要确认线上已有队列属性，否则可能触发队列参数不一致错误。

## 10. 推荐阅读顺序

1. `app/main.py`、`app/control/queue_client/client.py`：理解设备 Agent 启动和控制命令。
2. `app/control/supervisor.py`、`app/control/worker.py`：理解队列进程生命周期。
3. `app/queue/consumer.py`、`app/queue/message.py`：理解 RabbitMQ 消息进入业务的方式。
4. `app/core/task/router.py`、`app/core/task/dispatcher.py`：理解动态船司路由。
5. `app/core/task/base_task.py`：理解浏览器任务的统一生命周期。
6. `app/core/scheduler/account_session.py`、`app/core/browser/manager.py`：理解账号并发和浏览器资源管理。
7. `app/spider/ZIM/`、`app/spider/MSCGW/`：阅读具体业务实现。
8. `queue_control_platform/server/`：需要维护中心控制平台时再阅读其 API、事件和持久化实现。
